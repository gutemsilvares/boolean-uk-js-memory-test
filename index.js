// SetInterval

const numbers = [20, 40, 60, 100];
const result = numbers;

setTimeout(function () {
	alert("Remember this Numbers: " + numbers); //set once 5 seconds
}, 5000);

prompt("Put the Numbers: ");
// setTimeout(function () {

// }, 2000);

if (prompt === result) {
	alert("The number is in order: ");
} else {
	alert("The numbers is not in order: ");
}
